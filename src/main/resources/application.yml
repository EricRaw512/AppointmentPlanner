spring:
    datasource:
        url: jdbc:mysql://localhost:3306/appointment_planner
        username: admin
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        database-platform: org.hibernate.dialect.MySQLDialect
        hibernate:
            ddl-auto: create